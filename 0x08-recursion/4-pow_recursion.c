#include "main.h"
#include <math.h>
/**
 * _pow_recursion - prototype
 * @x: parameter
 * @y: parameter
 * Description: prints power of a number
 * Return: poweer
 */
int _pow_recursion(int x, int y)
{
	return (pow(x, y));
}
