#include "main.h"
#include <math.h>
/**
 * _sqrt_recursion - prototype
 * @n: parameter
 * Return: 0 success else -1
 */
int _sqrt_recursion(int n)
{
	if (sqrt(n))
	{
		return (sqrt(n));
	}
	else
		return (-1);
}
