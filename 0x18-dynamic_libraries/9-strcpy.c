#include "main.h"
#include<string.h>
/**
 * _strcpy - prototype
 * @dest: parameter
 * @src: parameter
 * Description: copies string to another
 * Return: 0 success
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
