#include"main.h"
#include<stdlib.h>
/**
 * _abs - prints the absolute value of a number.
 *
 * @b: parameter to be checked for absolute value.
 *
 * Description: returns the absolute number
 * Return: 0 (success)
 **/
int _abs(int b)
{
	int k;

	k = abs(b);
	return (k);
}
