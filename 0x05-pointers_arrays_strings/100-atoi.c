#include "main.h"
#include <stdlib.h>
/**
 * _atoi - prototype
 * @s: parameter
 * Description: converts string to integeer
 * Return: 0 (success)
 */
int _atoi(char *s)
{
	long long int i;

	i = atoi(s);
	return (i);
}
