#include "main.h"
#include <stdlib.h>
/**
 * _atoi - prototype
 * @s: parameter
 * Description: converts string to integeer
 * Return: 0 (success)
 */
int _atoi(char *s)
{
	void *p;
	signed long int = i

	*p = i;
	*p = atoi(s);
	return (*p);
}
