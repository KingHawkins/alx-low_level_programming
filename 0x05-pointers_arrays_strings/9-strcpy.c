#include "main.h"
#include<string.h>
/**
 * _strcpy - prototype
 * @dest: parameter
 * @src: parameter
 * Description: copies string to another
 * Return: 0 success
 */
char *_strcpy(char *dest, char *src)
{
	char s1, s2;

	*dest = s1;
	*src = s2;
	strcpy(s1, s2);
	return (*dest);
}
