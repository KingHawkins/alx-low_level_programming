#include <stdio.h>
#include <unistd.h>
/**
 * main - implementation of funtion happens here.
 *
 * Return: Always 0 (success)
 **/
int main(void)
{
	int a;

	for (a = 'z'; a >= 'a'; a--)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
